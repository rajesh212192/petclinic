pipeline {
    agent any
    stages {
        stage ('shell script') {
            steps {
                 sh '''#!/bin/bash
                 sleep 60
                  echo "Hello Delcarative pipeline"'''
            }
        }
    }
}
